[package]
name        = "imgui-basic"
version     = "0.1.0"
description = "Headless consumer for the imgui.core module"
license     = "MIT"

[toolchain]
default = "llvm@20.1.7"

[dependencies]
imgui = { path = "../.." }
